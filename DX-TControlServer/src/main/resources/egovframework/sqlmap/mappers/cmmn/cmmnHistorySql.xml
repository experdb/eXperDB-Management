<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper   PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cmmnHistorySQL">


	<insert id="insertHistoryDbTree" parameterType="historyVO">
		/**
		* Query ID : insertHistoryDbTree
		* Description : DB Server Tree 이력 등록
		* Author : 김주영
		**/

		INSERT INTO T_SYSWRK_G
		(
		  WRK_SN
		, USR_ID
		, EXE_DTL_CD
		, EXEDTM
		, LGI_IPADR
		)
		VALUES
		(
		  nextval('q_syswrk_g_01')
		, #{usr_id}
		, 'DX-T0005'
		, clock_timestamp()
		, #{lgi_ipadr}
		)
	</insert>

	<insert id="insertHistoryDbServer" parameterType="historyVO">
		/**
		* Query ID : insertHistoryDbServer
		* Description : DB Server 이력 등록
		* Author : 김주영
		**/

		INSERT INTO T_SYSWRK_G
		(
		  WRK_SN
		, USR_ID
		, EXE_DTL_CD
		, EXEDTM
		, LGI_IPADR
		)
		VALUES
		(
		  nextval('q_syswrk_g_01')
		, #{usr_id}
		, 'DX-T0006'
		, clock_timestamp()
		, #{lgi_ipadr}
		)
	</insert>

	<insert id="insertHistoryDbServerRegPopup" parameterType="historyVO">
		/**
		* Query ID : insertHistoryDbServerRegPopup
		* Description : DB Server 등록팝업 이력 등록
		* Author : 김주영
		**/

		INSERT INTO T_SYSWRK_G
		(
		  WRK_SN
		, USR_ID
		, EXE_DTL_CD
		, EXEDTM
		, LGI_IPADR
		)
		VALUES
		(
		  nextval('q_syswrk_g_01')
		, #{usr_id}
		, 'DX-T0007'
		, clock_timestamp()
		, #{lgi_ipadr}
		)
	</insert>

	<insert id="insertHistoryDbServerI" parameterType="historyVO">
		/**
		* Query ID : insertHistoryDbServerI
		* Description : DB Server 등록팝업 저장 등록
		* Author : 김주영
		**/

		INSERT INTO T_SYSWRK_G
		(
		  WRK_SN
		, USR_ID
		, EXE_DTL_CD
		, EXEDTM
		, LGI_IPADR
		)
		VALUES
		(
		  nextval('q_syswrk_g_01')
		, #{usr_id}
		, 'DX-T0007_01'
		, clock_timestamp()
		, #{lgi_ipadr}
		)
	</insert>
	
	<insert id="insertHistoryDatabase" parameterType="historyVO">
		/**
		* Query ID : insertHistoryDatabase
		* Description : Database조회 이력 등록
		* Author : 김주영
		**/

		INSERT INTO T_SYSWRK_G
		(
		  WRK_SN
		, USR_ID
		, EXE_DTL_CD
		, EXEDTM
		, LGI_IPADR
		)
		VALUES
		(
		  nextval('q_syswrk_g_01')
		, #{usr_id}
		, 'DX-T0009'
		, clock_timestamp()
		, #{lgi_ipadr}
		)
	</insert>

</mapper>
