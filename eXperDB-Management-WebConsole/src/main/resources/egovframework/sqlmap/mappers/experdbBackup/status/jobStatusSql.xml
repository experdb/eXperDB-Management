<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper   PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="backupDBsql">

	<select id="selectJobStatusList" resultType="JobStatusVO">
			SELECT 
				UUID
				, JOBNAME
				, JOBTYPE
				, JOBMETHOD
				, TARGETNAME
				, BACKUPLOCATION
				, (SELECT LOCATION FROM BACKUPLOCATION WHERE UUID = BACKUPLOCATION) AS BACKUPLOCATION_NM
				, ISREPEAT
				, JOBSTATUS
				, LASTRESULT
				, PRIORITY
				, TEMPLATEID
			FROM JOBQUEUE		
	</select>
		
</mapper>